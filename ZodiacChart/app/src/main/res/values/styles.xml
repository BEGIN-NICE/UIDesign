<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorOrange</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="style0">
        <item name="android:textSize">16dp</item>
        <item name="android:textColor">@color/colorBlack</item>
    </style>

    <style name="style1">
        <item name="android:textSize">30dp</item>
        <item name="android:textColor">@color/colorOrange</item>
    </style>

    <style name="style2">
        <item name="android:textSize">13dp</item>
        <item name="android:textColor">@color/colorBlack</item>
        <item name="android:typeface">normal</item>
    </style>

    <style name="RatingBar" parent="Base.Widget.AppCompat.ActionBar">
        <item name="android:progressDrawable">@drawable/designratingbar</item>
        <item name="android:maxHeight">18dp</item>
        <item name="android:minHeight">18dp</item>
    </style>

<!--    <style name="RatingBar" parent="@android:style/Widget.Holo.RatingBar.Indicator">
        <item name="colorControlNormal">@color/colorGray</item>
        <item name="colorControlActivated">@color/colorOrange</item>
        <item name="android:progressDrawable">@drawable/designratingbar</item>
    </style>-->

</resources>
